---

spring:
  application:
    name: roster
  cloud:
    config:
      uri: ${vcap.services.config-server.credentials.uri}
      
management:
  security:
    enabled: false
      
security:
  basic:
    enabled: false
      
eureka:
  client:
    serviceUrl:
      defaultZone: http://Dstashek:Snoopy123@eureka-dstashek-0_0_1-rc_1.cfapps.io/eureka